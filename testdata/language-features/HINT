  ok
  wrong

X

  syntactic
    semantic - reduced properly
      usage in values, functions, expressions, types

language features:
  done - comment
  done - language pragma
  syntax:
    done - literals
    done - tuples
    done - global value defs
    valid indentation
      done - global defs
      multiline def
      multi equation function def
      let in
      where
      case
      data def
      if then else
      list comprehension

    done - let in
    done - if then else
    done - lambda expression
    done - case of
    type signature - global, local, forall, hidden parameters, type parameters, wildcard, context
    patterns
    guards - in expressions
    list comprehension
    operators
    where - in value, in expressions

    definition order
    recursion
    mutual recursion - vary def order, vary on def placing (insert other code between mutual rec functions"

    data GADT and ADT
    parenthesis - nested
    fixity
    `infix` notation 
    widcards - in pattern, in type
    @ patterns
    builtins
    records
    swizzling
    hidden parameters
    type synonyms
    @ type parameters

  module name where
    done - with and without export list
    including exporing itself and other imported modules

  import
    done - hiding list
    done - import list
    qualified import
    qualified import as
    import as
